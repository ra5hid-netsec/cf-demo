# Cloudflare Demo App

A simple Flask app for demonstrating Cloudflare security features (Rate Limiting, WAF, Managed Challenge, etc.)

### Endpoints
- `/` → health check
- `/login` → POST (simple auth)
- `/api` → sample API endpoint
